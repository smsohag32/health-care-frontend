import { Button } from "@/components/ui/button";

const Login = () => {
   return (
      <div className=" flex items-center justify-center min-h-screen">
         <Button  className="!py-3 !px-10">Login</Button>
      </div>
   );
};

export default Login;
